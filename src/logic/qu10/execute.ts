
/**
 * 地形の高さマップを受け取り、溜まる雨水の総量を計算します。
 *
 * @param heights 各柱の高さを表す配列
 * @returns 溜まった水の総量
 * @throws {Error} 負の高さが含まれている場合
 */
export const main = (heights: number[]): number => {
  return Number(heights.join());
};
