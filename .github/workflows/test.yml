name: PR Trigger Test

on:
  pull_request:
    types: [opened]

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World! The PR trigger is working."